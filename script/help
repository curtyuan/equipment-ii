#!/usr/bin/env zsh
set -e

repo_dir="${0:A:h:h}"
source "${repo_dir}/ii.plugin.zsh"

for topic in ${(f)"$(ii_help_topics)"}; do
  print "===== ii ${topic} ====="
  if [[ "$topic" == "help" ]]; then
    ii help
  else
    ii help "$topic"
  fi
  print
done
